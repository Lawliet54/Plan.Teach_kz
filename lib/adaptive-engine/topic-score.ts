export function calculateTopicMastery(items:{masteryScore:number;weight:number}[]){const totalWeight=items.reduce((sum,item)=>sum+item.weight,0);if(!totalWeight)return 0;return Math.round(items.reduce((sum,item)=>sum+item.masteryScore*item.weight,0)/totalWeight*100)/100}
export function calculateFinalTopicScore(input:{checkpointScore:number;practiceScore:number;laboratoryScore?:number|null;appliedTaskScore:number}){const {checkpointScore,practiceScore,laboratoryScore,appliedTaskScore}=input;return laboratoryScore==null?Math.round((checkpointScore*.6+practiceScore*.3+appliedTaskScore*.1)*100)/100:Math.round((checkpointScore*.5+practiceScore*.25+laboratoryScore*.15+appliedTaskScore*.1)*100)/100}
